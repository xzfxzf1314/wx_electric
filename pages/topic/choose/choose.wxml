<!-- 选择级别和科目列表的界面 -->

<view class="list">

  <view class="item" wx:for="{{listData}}" wx:key="levelId" wx:for-item="item" 
  data-levelId="{{item.levelId}}" data-subjectId="{{item.subjectId}}" 
  data-levelDesc="{{item.levelDesc}}" data-subjectTitle="{{item.subjectTitle}}" bindtap="select" >

    <button class="{{item.isSelect ? 'btn-select' : 'btn'}}" hover-class="hover">{{item.levelDesc + ' ' + item.subjectTitle}}</button>
  </view>



</view>